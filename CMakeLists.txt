# first line of CMakeLists to indicate the cmake version requriement
cmake_minimum_required(VERSION 3.21.3)

# name the project
project(cmake_1bascis)

# generate executable file with which cpp file
add_executable(out main.cpp)